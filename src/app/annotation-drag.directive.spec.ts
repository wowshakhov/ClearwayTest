import { AnnotationDragDirective } from './annotation-drag.directive';

describe('AnnotationDragDirective', () => {
  it('should create an instance', () => {
    const directive = new AnnotationDragDirective();
    expect(directive).toBeTruthy();
  });
});
